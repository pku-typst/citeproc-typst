---
interface Category {
  name: string;
  total: number;
  compiled: number;
}

interface Props {
  categories: Category[];
}

const { categories } = Astro.props;
---

<details class="my-4">
  <summary class="cursor-pointer font-medium py-2 hover:text-primary">
    📊 按类别查看
  </summary>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-2 mt-4">
    {
      categories.map((cat) => (
        <div class="flex justify-between items-center px-3 py-2 bg-card border border-border rounded text-sm">
          <span class="font-medium">{cat.name}</span>
          <span class="text-success">
            {cat.compiled}/{cat.total}
          </span>
        </div>
      ))
    }
  </div>
</details>
